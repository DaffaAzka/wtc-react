export type TableInformationData = {
  name: string;
  value: string;
};

export type ManagementData = {
  name: string;
  url: string;
}

export type SelectItems = {
  id: string | number;
  name: string;
};
